==========
AsciiTable
==========

format table to reStructedText Table

.. note::

    all inputed data must be string.

Usage
=====
::

    data = [['a', 'b', 'c'], ['e', 'f', 'g']]
    t = AsciiTable(data)
    print(t.table)
